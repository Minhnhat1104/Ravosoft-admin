// material-ui
import { alpha, Theme } from '@mui/material/styles';

export default function Button(theme: Theme) {
  return {
    MuiButton: {
      defaultProps: {},
      styleOverrides: {},
    },
  };
}
